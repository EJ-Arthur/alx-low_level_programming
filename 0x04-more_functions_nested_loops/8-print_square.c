#include "main.h"

/**
 * print_square - prints a square
 * Return: void
 * @size: number of times # must be printed
 */

void print_square(int size)
{
	int i;

	if (size <= 0)
		_putchar('\n');
	else
	{
		for (i = 0; i < size; i++)
		{
			while (n > 0)
			{
				_putchar('#');
				n -= 1;
			}

			_putchar('\n');
		}
	}
}
