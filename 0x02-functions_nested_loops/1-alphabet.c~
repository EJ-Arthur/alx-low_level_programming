#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0
 */

int main(void)
{
	print_alphabet();
	_putchar('\n');

	return (0);
}

int print_alphabet(void)
{
	char lett = 'a';
	while (lett <= 'z')
	{
		_putchar(lett);
		lett++;
	}

	return (0);
}
