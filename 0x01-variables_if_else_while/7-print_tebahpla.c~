#include <stdio>

/**
 *main - prints the lowercase alphabet inreverse followed by a new line
 *
 *Return: Always 0 
 */

int main(void)
{
	char letter = 'z';
	
	while (letter >= 'a')
	{
		putchar(letter);
		letter--;
	}

	putchar('\n');
}
